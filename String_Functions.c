// strlength -- > get the length
// strcpy and strncpy --> copy one string to another
// strcat and strncat combine two character strings together
// strcmp and strncmp --> determine if two chacter strings are  equal


#include <stdio.h>
#include <string.h>



int main () {
    char mystring[] = "my string";

    printf("LENGTH of string is %d",strlen(mystring));
}

