#include <stdio.h>
// Array is very common to store many data values of a specified type
// array is fixed number of data items
//cannot mix data
int main (){
    int myNumbers[] = { 1,2,3,4,5,6,7};
    for ( int i =0 ;i <7;i++){
        printf(" %d ",myNumbers[i]);
    }
}

